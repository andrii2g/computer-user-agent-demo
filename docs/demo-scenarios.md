# Demo Scenarios

## Hello artifact

Creates a script, runs it, and stores the output in `result.txt`.

## Fix and retry

Creates a broken script, inspects the error, fixes it, and runs again.

## Mini report

Creates sample CSV data, writes a summarizer, runs it, and produces `report.md`.
